{
  "AllowMultiple": "false",
  "Description": "\"Upgraded the accretion disk to a 2000-particle system using Points and PointsMaterial with Keplerian speed simulation, dynamic spiral-in on transition, and event horizon scale animation.\"",
  "EndLine": "214",
  "Instruction": "\"Insert ParticleAccretionDisk component and update the BlackHole component to use it along with core scaling animation.\"",
  "ReplacementContent": "\"// \u2500\u2500\u2500 Particle Accretion Disk \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nconst ParticleAccretionDisk: React.FC = () => {\\n  const pointsRef = useRef<THREE.Points>(null);\\n  const homeTransitionState = useSceneStore((s) => s.homeTransitionState);\\n  const convergeElapsed = useRef(0);\\n  const particleCount = 2000;\\n\\n  // Generate particle coordinate data once\\n  const [positions, colors, sizes, particleData] = useMemo(() => {\\n    const pos = new Float32Array(particleCount * 3);\\n    const cols = new Float32Array(particleCount * 3);\\n    const szs = new Float32Array(particleCount);\\n    const data = [];\\n\\n    const colorPalette = (r: number) => {\\n      // Inner: bright white-hot -> orange-yellow\\n      if (r < 1.3) {\\n        return new THREE.Color('#ffffff').lerp(new THREE.Color('#ffc14d'), (r - 0.8) / 0.5);\\n      } \\n      // Middle: hot orange -> deep red\\n      else if (r < 2.3) {\\n        return new THREE.Color('#ffa64d').lerp(new THREE.Color('#ff2200'), (r - 1.3) / 1.0);\\n      } \\n      // Outer: magenta -> dark purple\\n      else {\\n        return new THREE.Color('#ff33cc').lerp(new THREE.Color('#4d0080'), (r - 2.3) / 1.2);\\n      }\\n    };\\n\\n    for (let i = 0; i < particleCount; i++) {\\n      const r = 0.8 + Math.random() * 2.7; // radius from 0.8 to 3.5\\n      const theta = Math.random() * Math.PI * 2;\\n      const speed = 0.28 / Math.sqrt(r); // Keplerian speed\\n      const yOffset = (Math.random() - 0.5) * 0.14 * (1.0 - r / 3.5); // taper thickness\\n\\n      // Save initial state\\n      data.push({ r, theta, speed, yOffset });\\n\\n      // Position\\n      pos[i * 3] = Math.cos(theta) * r;\\n      pos[i * 3 + 1] = yOffset;\\n      pos[i * 3 + 2] = Math.sin(theta) * r;\\n\\n      // Color\\n      const c = colorPalette(r);\\n      cols[i * 3] = c.r;\\n      cols[i * 3 + 1] = c.g;\\n      cols[i * 3 + 2] = c.b;\\n\\n      // Size\\n      szs[i] = 0.025 + Math.random() * 0.045;\\n    }\\n\\n    return\n<truncated 6769 bytes>",
  "StartLine": "127",
  "TargetContent": "\"// \u2500\u2500\u2500 BlackHole Component \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nconst BlackHole: React.FC = () => {\\n  const disk1Ref  = useRef<THREE.Mesh>(null);\\n  const disk2Ref  = useRef<THREE.Mesh>(null);\\n  const disk3Ref  = useRef<THREE.Mesh>(null);\\n  const ringRef   = useRef<THREE.Sprite>(null);\\n\\n  // Textures\\n  const photonTex = useMemo(() => typeof window !== 'undefined' ? createPhotonRingTex()   : null, []);\\n  const diskTex1  = useMemo(() => typeof window !== 'undefined'\\n    ? createDiskTex('#ffffff', '#ff9933', '#cc2200') : null, []);\\n  const diskTex2  = useMemo(() => typeof window !== 'undefined'\\n    ? createDiskTex('#ffd0ff', '#cc44ee', '#441166') : null, []);\\n  const glowTex   = useMemo(() => typeof window !== 'undefined' ? createRadialGlow('#ff6622', 512) : null, []);\\n\\n  useFrame(({ clock }) => {\\n    const t = clock.getElapsedTime();\\n    // Disks spin at different rates\\n    if (disk1Ref.current)  disk1Ref.current.rotation.z  =  t * 0.30;\\n    if (disk2Ref.current)  disk2Ref.current.rotation.z  = -t * 0.18;\\n    if (disk3Ref.current)  disk3Ref.current.rotation.z  =  t * 0.12;\\n    // Photon ring breathes\\n    if (ringRef.current) {\\n      const s = 2.05 + Math.sin(t * 1.1) * 0.04;\\n      ringRef.current.scale.set(s, s, 1);\\n    }\\n  });\\n\\n  return (\\n    <group>\\n      {/* \u2550\u2550 1. Wide outer ambient haze (background, no depth) \u2550\u2550 */}\\n      {glowTex && (\\n        <sprite scale={[6.0, 6.0, 1]} renderOrder={0}>\\n          <spriteMaterial map={glowTex} color=\\\"#ff5511\\\" transparent opacity={0.12}\\n            blending={THREE.AdditiveBlending} depthTest={false} depthWrite={false} />\\n        </sprite>\\n      )}\\n\\n      {/* \u2550\u2550 2. Accretion disk 3 \u2014 faint outer blue-purple \u2550\u2550 */}\\n      {diskTex2 && (\\n        <mesh ref={disk3Ref} rotation={[Math.PI / 2.8, 0.5, 0]} renderOrder={1}>\\n          <planeGeometry args={[7.0, 0.9]} />\\n          <meshBasicMaterial\n<truncated 1280 bytes>",
  "TargetFile": "\"/home/deist/Downloads/Work/dnbd-next/src/components/scene/HomeScene.tsx\"",
  "toolAction": "\"Replacing old planes with points particle accretion disk\"",
  "toolSummary": "\"Upgrade HomeScene.tsx disk to points\""
}