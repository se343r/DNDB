{
  "AllowMultiple": "false",
  "Description": "\"Removed the rotating plane meshes, textures, refs, and animation logic that created the colored light streaks radiating from the black hole.\"",
  "EndLine": "304",
  "Instruction": "\"Remove the 3 rotating accretion disk plane meshes, their texture helpers, refs, and animation loops.\"",
  "ReplacementContent": "\"// \u2500\u2500\u2500 BlackHole Component \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nconst BlackHole: React.FC = () => {\\n  const ringRef   = useRef<THREE.Sprite>(null);\\n  const coreRef   = useRef<THREE.Mesh>(null);\\n\\n  const homeTransitionState = useSceneStore((s) => s.homeTransitionState);\\n  const convergeElapsed = useRef(0);\\n\\n  // Textures\\n  const photonTex = useMemo(() => typeof window !== 'undefined' ? createPhotonRingTex()   : null, []);\\n  const glowTex   = useMemo(() => typeof window !== 'undefined' ? createRadialGlow('#ff6622', 512) : null, []);\\n\\n  useFrame(({ clock }, delta) => {\\n    const t = clock.getElapsedTime();\\n\\n    // Animate core and photon ring during transition\\n    let scale = 1.0;\\n    if (homeTransitionState === 'converging') {\\n      convergeElapsed.current += delta;\\n      const progress = Math.min(1.0, convergeElapsed.current / 1.3);\\n      // Core expands slightly then collapses to 0\\n      scale = Math.max(0, 1.0 + 0.35 * Math.sin(progress * Math.PI) - progress * 1.05);\\n    } else if (homeTransitionState === 'shooting') {\\n      scale = 0;\\n    } else {\\n      convergeElapsed.current = 0;\\n    }\\n\\n    if (coreRef.current) {\\n      coreRef.current.scale.set(scale, scale, scale);\\n    }\\n\\n    // Photon ring breathes & scales down during transition\\n    if (ringRef.current) {\\n      const s = (2.05 + Math.sin(t * 1.1) * 0.04) * scale;\\n      ringRef.current.scale.set(s, s, 1);\\n    }\\n  });\\n\\n  return (\\n    <group>\\n      {/* \u2550\u2550 1. Wide outer ambient haze (background, no depth) \u2550\u2550 */}\\n      {glowTex && (\\n        <sprite scale={[6.0, 6.0, 1]} renderOrder={0}>\\n          <spriteMaterial map={glowTex} color=\\\"#ff5511\\\" transparent opacity={0.12}\\n            blending={THREE.AdditiveBlending} depthTest={false} depthWrite={false} />\\n        </sprite>\\n      )}\\n\\n      {/* \u2550\u2550 3D Particle Accretion Disk (Keplerian orbits) \u2550\u2550 */}\\n      <ParticleAccr\n<truncated 321 bytes>",
  "StartLine": "242",
  "TargetContent": "\"// \u2500\u2500\u2500 BlackHole Component \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nconst BlackHole: React.FC = () => {\\n  const disk1Ref  = useRef<THREE.Mesh>(null);\\n  const disk2Ref  = useRef<THREE.Mesh>(null);\\n  const disk3Ref  = useRef<THREE.Mesh>(null);\\n  const ringRef   = useRef<THREE.Sprite>(null);\\n  const coreRef   = useRef<THREE.Mesh>(null);\\n\\n  const homeTransitionState = useSceneStore((s) => s.homeTransitionState);\\n  const convergeElapsed = useRef(0);\\n\\n  // Textures\\n  const photonTex = useMemo(() => typeof window !== 'undefined' ? createPhotonRingTex()   : null, []);\\n  const diskTex1  = useMemo(() => typeof window !== 'undefined'\\n    ? createDiskTex('#ffffff', '#ff9933', '#cc2200') : null, []);\\n  const diskTex2  = useMemo(() => typeof window !== 'undefined'\\n    ? createDiskTex('#ffd0ff', '#cc44ee', '#441166') : null, []);\\n  const glowTex   = useMemo(() => typeof window !== 'undefined' ? createRadialGlow('#ff6622', 512) : null, []);\\n\\n  useFrame(({ clock }, delta) => {\\n    const t = clock.getElapsedTime();\\n    // Disks spin at different rates\\n    if (disk1Ref.current)  disk1Ref.current.rotation.z  =  t * 0.30;\\n    if (disk2Ref.current)  disk2Ref.current.rotation.z  = -t * 0.18;\\n    if (disk3Ref.current)  disk3Ref.current.rotation.z  =  t * 0.12;\\n\\n    // Animate core and photon ring during transition\\n    let scale = 1.0;\\n    if (homeTransitionState === 'converging') {\\n      convergeElapsed.current += delta;\\n      const progress = Math.min(1.0, convergeElapsed.current / 1.3);\\n      // Core expands slightly then collapses to 0\\n      scale = Math.max(0, 1.0 + 0.35 * Math.sin(progress * Math.PI) - progress * 1.05);\\n    } else if (homeTransitionState === 'shooting') {\\n      scale = 0;\\n    } else {\\n      convergeElapsed.current = 0;\\n    }\\n\\n    if (coreRef.current) {\\n      coreRef.current.scale.set(scale, scale, scale);\\n    }\\n\\n    // Photon ring breathes & scal\n<truncated 2267 bytes>",
  "TargetFile": "\"/home/deist/Downloads/Work/dnbd-next/src/components/scene/HomeScene.tsx\"",
  "toolAction": "\"Removing old plane meshes from BlackHole component\"",
  "toolSummary": "\"Edit HomeScene.tsx to remove planes\""
}