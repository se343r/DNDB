{
  "AllowMultiple": "false",
  "Description": "\"Refactored ShootBeam to render a dual-layer vertical laser cylinder beam that shoots down off the bottom of the screen, implementing the vertical shooting beam transition.\"",
  "EndLine": "497",
  "Instruction": "\"Replace ShootBeam with a vertical cylinder-based beam shooting down, and remove glowTex from it.\"",
  "ReplacementContent": "\"const ShootBeam: React.FC<{\\n  active: boolean;\\n  onComplete: () => void;\\n}> = ({ active, onComplete }) => {\\n  const beamRef = useRef<THREE.Group>(null);\\n  const materialRef = useRef<THREE.MeshBasicMaterial>(null);\\n  const glowMaterialRef = useRef<THREE.MeshBasicMaterial>(null);\\n  const elapsed   = useRef(0);\\n  const done      = useRef(false);\\n\\n  useEffect(() => { if (active) { elapsed.current = 0; done.current = false; } }, [active]);\\n\\n  useFrame((_, delta) => {\\n    if (!active || !beamRef.current || done.current) return;\\n    elapsed.current += delta;\\n    const duration = 0.5; // 0.5s shoot duration\\n    const t = Math.min(1.0, elapsed.current / duration);\\n    \\n    // Easing: quadratic ease-in\\n    const progress = t * t;\\n    const maxLength = 12.0; // Shoot off-screen\\n    const currentLength = maxLength * progress;\\n    \\n    if (beamRef.current) {\\n      // Scale cylinder height\\n      beamRef.current.scale.set(1.0, currentLength / 8.0, 1.0);\\n      // Move cylinder center down so top stays at (0,0,0)\\n      beamRef.current.position.y = -currentLength / 2;\\n      \\n      // Fade out opacity near the end\\n      if (t > 0.7) {\\n        const fade = (1.0 - t) / 0.3;\\n        if (materialRef.current) materialRef.current.opacity = Math.max(0, fade);\\n        if (glowMaterialRef.current) glowMaterialRef.current.opacity = Math.max(0, fade * 0.4);\\n      } else {\\n        if (materialRef.current) materialRef.current.opacity = 1.0;\\n        if (glowMaterialRef.current) glowMaterialRef.current.opacity = 0.4;\\n      }\\n    }\\n    \\n    if (t >= 1.0 && !done.current) {\\n      done.current = true;\\n      onComplete();\\n    }\\n  });\\n\\n  if (!active) return null;\\n  return (\\n    <group ref={beamRef}>\\n      {/* 1. Core white laser beam */}\\n      <mesh>\\n        <cylinderGeometry args={[0.08, 0.08, 8.0, 16]} />\\n        <meshBasicMaterial ref={materialRef} color=\\\"#ffffff\\\" transparent depthWrite={false} />\\n      </mesh>\\n      {/* 2. Outer blue/violet glow beam */}\\n      <mesh>\\n        <cylinderGeome\n<truncated 2443 bytes>",
  "StartLine": "405",
  "TargetContent": "\"const ShootBeam: React.FC<{\\n  active: boolean;\\n  glowTex: THREE.CanvasTexture | null;\\n  onComplete: () => void;\\n}> = ({ active, glowTex, onComplete }) => {\\n  const spriteRef = useRef<THREE.Sprite>(null);\\n  const elapsed   = useRef(0);\\n  const done      = useRef(false);\\n\\n  useEffect(() => { if (active) { elapsed.current = 0; done.current = false; } }, [active]);\\n\\n  useFrame((_, delta) => {\\n    if (!active || !spriteRef.current || done.current) return;\\n    elapsed.current += delta;\\n    const t = Math.min(1.0, elapsed.current / 0.45);\\n    const scale = THREE.MathUtils.lerp(0.5, 22.0, t * t);\\n    const opacity = t < 0.65 ? 1.0 : THREE.MathUtils.lerp(1.0, 0.0, (t - 0.65) / 0.35);\\n    spriteRef.current.scale.set(scale, scale, 1);\\n    spriteRef.current.position.z = THREE.MathUtils.lerp(0, 5.0, t);\\n    (spriteRef.current.material as THREE.SpriteMaterial).opacity = opacity;\\n    if (t >= 1.0 && !done.current) { done.current = true; onComplete(); }\\n  });\\n\\n  if (!active) return null;\\n  return (\\n    <sprite ref={spriteRef} scale={[0.5, 0.5, 1]}>\\n      <spriteMaterial map={glowTex ?? undefined} color=\\\"#ffffff\\\"\\n        transparent opacity={1} blending={THREE.AdditiveBlending} depthWrite={false} />\\n    </sprite>\\n  );\\n};\\n\\n// \u2500\u2500\u2500 HomeScene \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nexport const HomeScene: React.FC = () => {\\n  const homeTransitionState    = useSceneStore((s) => s.homeTransitionState);\\n  const setHomeTransitionState = useSceneStore((s) => s.setHomeTransitionState);\\n  const setAppPhase            = useSceneStore((s) => s.setAppPhase);\\n\\n  const glowTex = useMemo(() => typeof window !== 'undefined' ? createRadialGlow('#ffffff') : null, []);\\n\\n  const positionRefs = useMemo(\\n    () => Array.from({ length: 8 }, () => ({ current: new THREE.Vector3() })) as React.MutableRefObject<THREE.Vector3>[],\\n    []\\n  );\\n  const transit\n<truncated 1445 bytes>",
  "TargetFile": "\"/home/deist/Downloads/Work/dnbd-next/src/components/scene/HomeScene.tsx\"",
  "toolAction": "\"Modifying ShootBeam in HomeScene.tsx\"",
  "toolSummary": "\"Update ShootBeam component\""
}